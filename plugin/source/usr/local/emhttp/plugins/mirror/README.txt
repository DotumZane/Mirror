Mirror Unraid Plugin

This is an early installable test build.

It installs:
- /usr/local/emhttp/plugins/mirror
- /usr/local/sbin/mirrorctl
- /boot/config/plugins/mirror/config.json on first install

Do not point this build at important shares.
